# Auto-provisions the Prometheus datasource so Grafana needs no manual setup. The fixed uid
# ("prometheus") is what the dashboard JSON references, so the wiring survives a fresh container.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    uid: prometheus
    url: http://prometheus:9090
    isDefault: true
    editable: false
